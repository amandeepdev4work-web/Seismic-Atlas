# Current State

_Last updated: end of Phase 0 (scaffold complete)_

Scaffold is built and verified: `npm run build` and `tsc --noEmit` both pass
clean, and `npm run dev` serves a placeholder page at http://localhost:3000.
No map, no data, no components wired yet.

## Exists
- **Next.js 16.3 (App Router) + React 19 + TypeScript 7**, scaffolded by hand
  (no `create-next-app`), so `/docs` was left untouched.
- `app/layout.tsx` — root layout. Tab title comes from `metadata.title`,
  which reads `APP_NAME`.
- `app/page.tsx` — placeholder only: full-viewport dark panel, centered
  `APP_NAME` heading, "coming online" in smaller muted text below.
- `app/globals.css` — CSS custom properties for the dark theme
  (`--background` `#0b0f14`, `--foreground` `#e8edf2`, `--muted` `#7d8b99`).
- `lib/branding.ts` — exports `APP_NAME = "Seismic Atlas"`. Single source of
  truth for the product name; imported by both layout and page.
- `components/` — empty (`.gitkeep` only).
- `public/` — empty (`.gitkeep` only).
- `tsconfig.json` — strict mode on, `@/*` path alias mapped to project root.
- `next.config.ts` — empty config object, ready for later options.
- `.env.local.example` — contains `NEXT_PUBLIC_MAPBOX_TOKEN=` (unset).
- `.gitignore` — ignores `node_modules`, `.next`, `.vercel`, `.env.local`
  and friends. `.env.local.example` is deliberately *not* ignored.
- `AGENTS.md` / `CLAUDE.md` — auto-generated by Next 16 on first `dev` run.

## Dependencies installed
Runtime: `next`, `react`, `react-dom`, `mapbox-gl`, `papaparse`,
`@turf/turf`, `@mapbox/mapbox-gl-draw`.
Dev: `typescript`, `@types/node`, `@types/react`, `@types/react-dom`,
`@types/mapbox-gl`, `@types/papaparse`, `@types/mapbox__mapbox-gl-draw`.

All Phase 1–5 libraries are installed already, so no further `npm install`
is expected until the roadmap changes.

## Scripts
- `npm run dev` — dev server (Turbopack) on port 3000
- `npm run build` — production build
- `npm run start` — serve the production build
- `npm run typecheck` — `tsc --noEmit`

Note: `next lint` was removed in Next 16, so there is no `lint` script.
ESLint is not installed.

## Does not exist yet
- No Mapbox token is set. `.env.local` has not been created — it must be
  made by hand from `.env.local.example`.
- No map instance, no GeoJSON source/layer, no sample CSV in `public/`.
- Nothing from Phase 1 onward.
