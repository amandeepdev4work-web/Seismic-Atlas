/**
 * Single source of truth for the product name.
 * Import APP_NAME anywhere the name is displayed — never hardcode it,
 * so a rename is a one-line change here.
 */
export const APP_NAME = "Seismic Atlas";
